import { readFile } from 'node:fs/promises';import { walkFiles } from './lib/source-files.mjs';const roots=['apps/web/src/features/avatar-catalog/domain','apps/web/src/features/avatar-catalog/ports','apps/web/src/features/avatar-catalog/queries','apps/web/src/features/avatar-catalog/presentation','apps/web/src/features/avatar-catalog/integrations'];const banned=[/process\.env/,/\bwindow\b/,/\bdocument\b/,/Date\.now\s*\(/,/new Date\s*\(/,/Math\.random\s*\(/,/\bfetch\s*\(/];const bad=[];for(const r of roots)for(const f of await walkFiles(r)){if(!/\.(ts|tsx)$/.test(f))continue;const s=await readFile(f,'utf8');for(const re of banned)if(re.test(s))bad.push(`${f}: non-deterministic dependency ${re}`)}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('Pure layer check passed.')
