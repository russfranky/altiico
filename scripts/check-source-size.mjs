import { readFile } from 'node:fs/promises';import { walkFiles } from './lib/source-files.mjs';const bad=[];for(const r of ['apps/web/src','packages','scripts','tests'])for(const f of await walkFiles(r)){if(!/\.(ts|tsx|mjs|css)$/.test(f))continue;const s=await readFile(f,'utf8'),bytes=Buffer.byteLength(s),lines=s.split('\n').length,isCss=f.endsWith('.css');const maxB=isCss?12000:f.startsWith('scripts/')?4500:6000,maxL=f.includes('/app/')&&f.endsWith('/page.tsx')?80:120;if(bytes>maxB)bad.push(`${f}: ${bytes}>${maxB} bytes`);if(!isCss&&lines>maxL)bad.push(`${f}: ${lines}>${maxL} lines`)}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('Source size check passed.')
