import { readFile } from 'node:fs/promises';const port=await readFile('apps/web/src/features/avatar-catalog/ports/avatar-catalog.ts','utf8');const adapter=await readFile('apps/web/src/features/avatar-catalog/adapters/local/adapter.ts','utf8');for(const m of ['listAvatarSets','getAvatarSetBySlug','getAvatarByRoute'])if(!port.includes(m))throw Error(`Catalog port missing ${m}`);if(!adapter.includes('fixtureProvenance'))throw Error('Adapter must expose provenance');console.log('Catalog contract check passed.')
