import { readFile } from 'node:fs/promises';for(const [p,name] of [['contracts/catalog-evidence/hubzz-prealpha-staging-v1.schema.json','hubzz-prealpha-staging-v1'],['contracts/catalog-evidence/hubzz-prealpha-source-avatars-v1.schema.json','hubzz-prealpha-source-avatars-v1']]){const s=JSON.parse(await readFile(p,'utf8'));if(s.properties?.schema?.const!==name)throw Error(`${p}: discriminator mismatch`);for(const k of ['schema','schemaVersion','generatedAt','snapshotId'])if(!s.required?.includes(k))throw Error(`${p}: missing ${k}`)}console.log('Wire contract check passed (2 schemas).')
