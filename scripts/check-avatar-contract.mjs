import { readFile } from 'node:fs/promises';const d=await readFile('apps/web/src/features/avatar-catalog/domain/avatar.ts','utf8');const p=await readFile('apps/web/src/features/avatar-catalog/ports/avatar-catalog.ts','utf8');for(const t of ['AvatarDetail','AvatarProductIdentity','productAvatarId','avatarSlug','sourceAssetId','apiAvatarId','tokenId'])if(!d.includes(t))throw Error(`Avatar domain missing ${t}`);if(!p.includes('getAvatarByRoute'))throw Error('Port missing avatar lookup');console.log('Avatar contract check passed.')
