# Individual avatar detail

Canonical route: `/explore/avatar-sets/[setSlug]/avatars/[avatarSlug]`.

Keep product avatar ID, public slug, source asset ID, API avatar ID, token ID, chain, and contract as separate fields.

The profile can show source evidence and provenance. Studio owns engine rendering and QA.
