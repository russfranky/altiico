# Discovery data convergence

Legacy evidence can augment canonical product records through pure mapping functions.

It can fill empty source identity fields and replace evidence blocks.

It cannot overwrite product IDs, public slugs, API identity, display copy, publication state, or readiness.

Snapshot IDs are required explicitly. Reconciliation conflicts remain visible.
