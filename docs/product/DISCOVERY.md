# Public discovery

The discovery route lists avatar sets through `AvatarCatalogPort`.

Presentation does not know which adapter produced the records.

Search and filter calculations are pure queries. Missing records and source failures remain explicit states.
