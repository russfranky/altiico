# Visual references

The full-resolution design reference images are source-design artifacts, not runtime dependencies.

The branch includes production avatar crops under `apps/web/public/brand/`.

Add large design-reference PNGs later through the normal Git or LFS workflow when needed.
