# Token budget (loop-engineering)
suggested_daily_cap: 200000
per_iteration_cap: 40000
early_exit_required: true
note: The map refresh (loop_iterate.sh) is deterministic Python — ~0 tokens.
  Tokens are only spent when an agent works an items.json judgment task.
