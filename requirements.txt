# Runtime dependencies for the Altiico Catalog pipeline.
# Pinned to the supported Python 3.11 environment.
# Install: python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# extract_vrm_meta.py and the core GLB parsing path use the standard library.
# These dependencies support source adapters, on-chain reads, configuration,
# schema validation, and the test suite.

aiohttp==3.14.1
requests==2.34.2
PyYAML==6.0.3
jsonschema==4.26.0
web3==7.16.0
eth-utils==6.0.0
eth-abi==5.2.0

# Test and development
pytest==9.1.1
pytest-asyncio==1.4.0
